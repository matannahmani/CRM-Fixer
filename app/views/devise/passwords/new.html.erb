<div class="sessioncon">
  <%= link_to image_tag("logo.png"), root_path%>
  <div class="sessionform">
    <div class="sessionheadline">
      <h2 class="headline-text">איפוס סיסמה</h2>
    </div>
    <%= simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
      <%= f.error_notification %>

      <div class="form-inputs">
        <%= f.input :email,
                    required: true,
                    autofocus: true,
                    input_html: { autocomplete: "email" } %>
      </div>

      <div class="form-actions">
        <%= f.button :submit, "אפס סיסמה", class: "btn btn-sm btn-flatgreen" %>
        <%= link_to "חזרה", new_user_session_path, class: "btn btn-sm btn-flatgreen" %>
      </div>
    <% end %>
  </div>
</div>

