<div class="calltoaction">
    <%= link_to 'לצפייה בקריאות', calls_path, :class => "btn btn-flat" if (!current_user.nil? && current_user.issuper? ) %>
    <%= link_to 'מסך איזורים', regions_path, :class => "btn btn-flat" if (!current_user.nil? && current_user.issuper? ) %>
    <%= link_to 'מסך ערים', cities_path, :class => "btn btn-flat" if (!current_user.nil? && current_user.issuper? ) %>
    <%= link_to 'פתיחת קריאה', new_call_path, :class => "btn btn-flat" %>
    <%= link_to 'תעודת מתנדב', mypdf_path(:format => :pdf),target: :_blank, :class => "btn btn-flat" unless current_user.nil? %>
</div>
